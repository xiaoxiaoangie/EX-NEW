# 变更记录：{变更简述}

## 基本信息

| 项目 | 内容 |
|------|------|
| 变更日期 | {YYYY-MM-DD} |
| 变更类型 | 新增需求 / 需求变更 |
| 影响模块 | {模块名称} |
| 变更描述 | {一句话描述变更内容} |

## 需求说明

{描述变更的业务背景，原有机制是什么，变更后的规则是什么。用列表形式列出关键规则点。}

## 变更影响分析

### 接口变更（兼容/不兼容）

| 接口 | 变更内容 |
|------|----------|
| {接口编号} {接口名称} | {具体变更说明} |

### 新增错误码

| 错误码编号 | 常量名 | 说明 |
|-----------|--------|------|
| {编号} | {常量名} | {说明} |

### 数据库变更

{如有新增表，列出表结构：}

新增表 `{表名}`：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| {字段名} | {类型} | {约束} | {说明} |

索引：
- `{索引名}`：{索引类型} ({字段列表})

Liquibase changeSet id: {id}，位于 `{文件路径}`

{如有字段变更，列出变更：}

| 表名 | 变更类型 | 字段 | 说明 |
|------|----------|------|------|
| {表名} | 新增字段/修改字段/删除字段 | {字段名} | {说明} |

### 业务规则变更

| 规则 | 变更内容 |
|------|----------|
| {规则编号} | {变更说明，标注"新增"或"修改"} |

## 变更文件清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| {相对路径} | {文件用途说明} |

### 修改文件

| 文件路径 | 变更内容 |
|----------|----------|
| {相对路径} | {具体修改了什么} |

### 数据库迁移文件

| 文件路径 | 变更内容 |
|----------|----------|
| {资源路径} | {changeSet说明} |

### 文档变更

| 文件路径 | 变更内容 |
|----------|----------|
| {docs/路径} | {文档更新说明} |

## 接口调用方迁移指南

### {接口名称}（{接口编号}）

变更前：
```json
{HTTP方法} {路径}
{变更前的请求体JSON}
```

变更后：
```json
{HTTP方法} {路径}
{变更后的请求体JSON}
```

{如有响应变更，展示新增字段的响应示例：}

### {接口名称}响应变更

新增字段 `{字段名}`：
```json
{包含新字段的响应体JSON示例}
```

## 注意事项

1. **接口兼容性**：{说明哪些接口不兼容，调用方需同步修改}
2. **数据库迁移**：{部署前需执行的迁移操作}
3. **存量数据**：{已有数据的处理方案}
4. **其他提醒**：{如唯一性约束、调用方注意事项等}
